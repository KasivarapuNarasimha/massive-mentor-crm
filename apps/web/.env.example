# Frontend Environment Variables
# Copy to .env.local in apps/web

NEXT_PUBLIC_API_URL="http://localhost:4000/api"

# Server-only (middleware). Must match apps/api JWT_SECRET. NEVER use NEXT_PUBLIC_*.
JWT_SECRET=
