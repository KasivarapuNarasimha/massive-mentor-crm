NODE_ENV=production
NEXT_PUBLIC_API_URL=https://api.massivementor.in/api

# Server-only for demo host middleware JWT verification (must match API JWT_SECRET).
# NEVER prefix with NEXT_PUBLIC_.
JWT_SECRET=
