/**
 * Tailwind CSS v4 + Next.js 15 PostCSS Configuration
 *
 * Important: We use @tailwindcss/postcss instead of the old "tailwindcss" package
 * as a PostCSS plugin. The legacy tailwind.config.ts has been removed.
 */
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
